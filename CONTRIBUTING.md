# Contributing

Thanks for your interest in improving the loan risk system.

## Setup
```bash
make install
make test
```

## Guidelines
- Keep functions small and covered by a test in `tests/`.
- Run `make lint` and `make test` before opening a PR.
- Document any new risk thresholds in `src/config.py`.
